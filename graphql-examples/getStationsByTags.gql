query getStationsByTags {
  getStationsByTags(
    tags: ["chillout"]
  ) {
    items {
      _id
      name
      slug
      title
      website
      bitrate
      stream
      tags
      # disabled
      dateAdded
      dateUpdated
    }
  }
}

